problems in descending order (importance):
 -f no .env (paths half literal)
  like previously with running, now with .env access, works only with ctrl+shift+d
 -f no config

 -f generic calculator more than tf-idf
 /w messy file organization (mostly in General)
 -f lemmatization doesn't work
 /f simpler main

 -b initializing classes without their parameters
 -b only one logger + not enough logging
 -b library mltk, installing correctly
 -b closet of shelves isn't getting cleaned

 -b even though .gitignore contains .env, it doesn't ignore
 -b don't have simple way to disable annoying lemmatization (long in pycharm) turn on and off easily
 -b imports are general (import *) and await cleaning
 /w commented code awaits cleaning
 -b|gu testing are not organized (if __name__ instead)
 -b Some class names are confusing
 -b Some class names are too long
 -
 -
 -f in vscode doesn't run regularly, only debugs, with ctrl+shift+D 
 -b query handling is not very generic
 -b|gu type checking in vscode is very unhappy, unlike pycharm.
 -gu  there's only very simple main that does only one thing
































glossary:
  b = bad
  w = in work
  gu = given up not important enough (give up in style)
  f = fixed